{
  "name": "blog-applicatie",
  "private": true,
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i resources/css/app.css -o public/css/app.css --minify",
    "dev:css": "npx @tailwindcss/cli -i resources/css/app.css -o public/css/app.css --watch --poll"
  },
  "devDependencies": {
    "tailwindcss": "^4.0",
    "@tailwindcss/cli": "^4.0"
  }
}